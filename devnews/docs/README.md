# API Spec

`api-spec.yaml` is auto-generated from the running BE server.

## Update

```bash
# Run BE locally first, then:
curl http://localhost:8080/v3/api-docs.yaml -o devnews/docs/api-spec.yaml
```
